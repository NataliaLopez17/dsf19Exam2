package dsf19Exam2;

public class csc {

	/*
	 * Engineer Amy Grant might be running into trouble with the following code:
	 * 
	 * public int buggyQueue(Queue<String> Q){
	 * 
	 * int count = 0;
	 * 
	 * while (!Q.isEmpty()){
	 * 
	 * if (Q.front().equals("Trump")){
	 * 
	 * count++;
	 * 
	 * }
	 * 
	 * Q.enqueue(Q.dequeue());
	 * 
	 * }
	 * 
	 * }
	 * 
	 * What is the problem with this code?
	 * 
	 * Select one: a. None of the alternatives is right. b. Instruction
	 * Q.front().equals("Trump") will throw an exception if the queue is empty. c.
	 * Instruction Q.enqueue(Q.dequeue()) will cause an infinite loop. d. You cannot
	 * enqueue elements that you just dequeue. e. Nothing, this code has no bugs.
	 * Feedback Your answer is correct.
	 */

}
